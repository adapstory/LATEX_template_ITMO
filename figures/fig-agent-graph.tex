\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[
    node distance=1.6cm and 2.2cm,
    every node/.style={font=\small},
    agent/.style={rectangle, rounded corners=3pt, draw, minimum height=0.9cm, minimum width=2.8cm, align=center, fill=white},
    decision/.style={diamond, draw, minimum width=2.2cm, minimum height=1.8cm, align=center, fill=yellow!20, inner sep=1pt, font=\small},
    arr/.style={-stealth, thick},
  ]
    % Main vertical flow
    \node[agent] (target) {TargetAnalysis\\Agent};
    \node[agent, below=of target] (structure) {CourseStructure\\Agent};
    \node[decision, below=1.8cm of structure] (review) {human\_review\\\_structure};
    \node[agent, below=1.8cm of review] (content) {ContentCreator\\Agent};
    \node[agent, below=of content] (factcheck) {FactCheck\\Agent};
    \node[agent, below=of factcheck] (reviewer) {ReviewAgent};

    % Parallel branch
    \node[agent, right=3cm of review] (media) {MediaGeneration\\Agent};

    % Edges
    \draw[arr] (target) -- (structure);
    \draw[arr] (structure) -- (review);
    \draw[arr] (review) -- node[right, font=\footnotesize] {одобрено} (content);
    \draw[arr] (content) -- (factcheck);
    \draw[arr] (factcheck) -- (reviewer);

    % Rejection loop back
    \draw[arr] (review.west) -- ++(-1.5,0) |- node[left, pos=0.25, font=\footnotesize] {отклонено} (structure.west);

    % Parallel dashed to media
    \draw[arr, dashed] (review.east) -- node[above, font=\footnotesize] {} (media.west);
  \end{tikzpicture}
  \caption{Граф агентов ИИ-методиста (LangGraph StateGraph)}
  \label{fig:agent-graph}
\end{figure}
