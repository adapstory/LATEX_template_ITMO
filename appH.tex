% Ссылка в тексте: (приложение~И)
\chapter{ОЦЕНОЧНЫЕ АРТЕФАКТЫ И ТЕКУЩИЕ РЕЗУЛЬТАТЫ}

В~приложении собраны артефакты валидации, которые уже подготовлены
или~измерены в~текущей редакции диплома. Главный технический артефакт
текущей версии --- парный прогон одношаговой LLM (B2) и ИИ-методиста (B4)
на одном и том же
19-документном корпусе.
Этот прогон рассматривается как лабораторная проверка MVP: он показывает
готовность контура одношаговой LLM (B2) и ИИ-методиста (B4)
на контролируемом корпусе, но не используется
как доказательство превосходства над аналогами. Для такого вывода
подготовлена отдельная сравнительная проверка на открытых курсах $n \geq 30$.

\section*{И.1 --- Агрегированные результаты одношаговой LLM-конфигурации (B2)}

\begin{table}[h!]
  \caption{Агрегированные результаты лабораторного прогона одношаговой LLM-конфигурации (B2)}
  \label{tab:appendix-b2}
  \centering
  \begin{tabular}{|p{4.5cm}|p{2.0cm}|p{2.2cm}|p{2.5cm}|}
  \hline
  \textbf{Срез корпуса} & \textbf{$n$} & \textbf{Покрытие, \%} & \textbf{Доля $\geq 70\%$} \\
  \hline
  Все документы & 19 & 61.4 & 21.1\% \\
  \hline
  Продуктово-редакционные документы & 7 & 73.8 & 28.6\% \\
  \hline
  Корпоративные регламенты & 6 & 38.9 & 0\% \\
  \hline
  Учебные программы MIT~OCW & 6 & 69.5 & 33.3\% \\
  \hline
  \end{tabular}
\end{table}

Эти значения относятся к~одношаговой LLM-конфигурации (B2) из артефакта
базового сравнения и используются как технический
ориентир MVP. Для проверки вклада ИИ-методиста поверх этого базового ориентира
выполнен парный прогон одношаговой LLM (B2) и ИИ-методиста (B4):
для каждого документа \texttt{doc\_i}
сопоставлялись \texttt{B2\_result\_i} и \texttt{B4\_result\_i},
а итоговая дельта считалась по строкам, а не как сравнение независимых
средних.

\section*{И.2 --- Парный прогон одношаговой LLM (B2) и ИИ-методиста (B4)}

В~артефакте \texttt{paired\_b2\_b4\_experiment.json} сохранены 19 пар
результатов. Для каждого документа создавался отдельный \texttt{kb\_slug}
формата \texttt{diploma-b4-001}, \texttt{diploma-b4-002}, \ldots,
\texttt{diploma-b4-019}; в~B4 передавался ровно один исходный
Markdown-документ, а исходные доказательства сохранялись в каталоге
\texttt{paired\_b2\_b4\_runs/diploma-b4-XXX/}.

\begin{table}[h!]
  \caption{Сводка парного прогона одношаговой LLM (B2) и ИИ-методиста (B4) на одном корпусе}
  \label{tab:appendix-paired-b2-b4}
  \centering
  \begin{tabular}{|p{5.7cm}|p{2.5cm}|p{4.0cm}|}
  \hline
  \textbf{Метрика} & \textbf{Значение} & \textbf{Интерпретация} \\
  \hline
  Число пар документов & 19 & одношаговая LLM (B2) и ИИ-методист (B4) сравниваются построчно \\
  \hline
  Среднее покрытие Блума одношаговой LLM (B2) & 61.4\,\% & одношаговая базовая конфигурация ниже целевого порога \\
  \hline
  Среднее покрытие Блума ИИ-методиста (B4) & 83.3\,\% & B4 проходит целевой порог $\geq 70\,\%$ \\
  \hline
  Средняя парная дельта Блума & +21.9 п.п. & улучшение считается по каждому документу \\
  \hline
  Документы с положительной дельтой Блума & 15 из 19 & эффект наблюдается на большинстве корпуса \\
  \hline
  Привязка к источнику B4 & 100\,\% & все B4-запуски прошли контроль H2 по привязке к источнику \\
  \hline
  Знак качества B4 & 100\,\% «пройдено» & 19 из 19 запусков готовы к проверке человеком \\
  \hline
  \end{tabular}
\end{table}

\begin{table}[h!]
  \caption{Парная дельта Блума по категориям корпуса}
  \label{tab:appendix-paired-category}
  \centering
  \begin{tabular}{|p{4.8cm}|p{1.4cm}|p{2.0cm}|p{2.0cm}|p{2.2cm}|}
  \hline
  \textbf{Категория} & \textbf{$n$} & \textbf{B2, \%} & \textbf{B4, \%} & \textbf{Дельта} \\
  \hline
  Продуктово-редакционные документы & 7 & 73.8 & 83.3 & +9.5 п.п. \\
  \hline
  Корпоративные регламенты & 6 & 38.9 & 83.3 & +44.4 п.п. \\
  \hline
  Учебные программы MIT~OCW & 6 & 69.5 & 83.3 & +13.8 п.п. \\
  \hline
  \end{tabular}
\end{table}

\section*{И.3 --- Подготовленные оценочные артефакты}

\begin{table}[h!]
  \caption{Оценочные наборы текущего MVP}
  \label{tab:appendix-assets}
  \centering
  \begin{tabular}{|p{5.0cm}|p{2.0cm}|p{5.5cm}|}
  \hline
  \textbf{Артефакт} & \textbf{Объём} & \textbf{Назначение} \\
  \hline
  Измеренный корпус одношаговой LLM (B2) & 19 & подготовка курсов и~сравнение базовых подходов \\
  \hline
  Вопросно-ответные пары (\texttt{qa\_pairs.json}) & 30 & оценка faithfulness и answer relevancy \\
  \hline
  Фактуальные утверждения (\texttt{statements.json}) & 100 & расчёт precision / recall / F1 для~проверки фактов \\
  \hline
  Парный прогон B2/B4 & 19 пар & проверка прироста ИИ-методиста (B4) относительно одношаговой LLM (B2) на тех же документах \\
  \hline
  Лабораторная оценка & 30 вопросно-ответных пар + 100 утверждений & детерминированная проверка по схеме RAG и фактологическая проверка \\
  \hline
  \end{tabular}
\end{table}

\section*{И.4 --- Детерминированная проверка по схеме RAG и фактологическая проверка}

Скрипт \texttt{benchmarks.evaluation\_lab} выполнен в
детерминированном режиме \texttt{deterministic\_lexical}. Он не
вызывает внешние LLM и поэтому пригоден как регрессионный контроль.
На~30 вопросно-ответных парах получены метрики по схеме RAG:
faithfulness~=~0.335, answer relevancy~=~0.239,
context precision~=~0.567 и context recall~=~0.335. Эти значения
трактуются как диагностические, а не как основание для завышенного
вывода о~полном качестве поиска. В текущей ВКР RAG/eval-контур
работает как консервативный контроль перед проверкой человеком: качество поиска
требует расширения корпуса и экспертной разметки релевантных фрагментов.

На~100 фактуальных утверждениях контур проверки фактов дал:
precision~=~0.600, recall~=~0.900, F1~=~0.720.
Отдельно видно, что детектор хорошо ловит искажения типа
подмены сущностей, отрицания, временных искажений и большую часть числовых искажений,
но переотмечает часть корректных утверждений как подозрительные.
Это честно фиксирует текущий компромисс: система предпочитает
перестраховаться перед проверкой человеком, а не пропустить ошибку.

\section*{И.5 --- Машиночитаемые доказательные артефакты платформы}

В текущей кодовой базе ИИ-методиста уже предусмотрены поля, необходимые
для дипломной валидации:
\begin{itemize}
  \item ссылки на источники и происхождение блоков --- ссылочная
    привязка уроков к источникам;
  \item знак качества --- статус «пройдено» (\texttt{PASS}),
    «пройдено с замечаниями» (\texttt{PASS\_WITH\_NOTES}) или
    «не пройдено» (\texttt{FAIL});
  \item стоимость и качество --- стоимость, модель, токены и
    объяснение компромиссов цены и качества;
  \item снимок дипломной проверки --- снимок метрик H1--H3,
    пригодный для сохранения в экспериментальный корпус.
\end{itemize}

Фронтовый E2E-тест дипломной валидации проверяет отображение этих
полей в пользовательском контуре, а экспериментальные скрипты
превращают JSON-артефакты в отчёт контроля по гипотезам диплома
и парную таблицу B2/B4. Контроль готовности
\texttt{check\_thesis\_excellent\_readiness.py}
дополнительно запрещает возвращать в текст незавершённые утверждения
про внешние коммерческие обещания, закрытые материалы третьих лиц или
визуальные материалы как будто они являются измеренными результатами.

\section*{И.6 --- Подготовленная сравнительная проверка на открытых курсах}

Для расширения проверки за пределы 19-документной лабораторной проверки MVP
подготовлен воспроизводимый манифест \texttt{open\_course\_benchmark\_manifest.json}
и CSV-версия \texttt{open\_course\_benchmark\_manifest.csv}. Манифест
содержит 39 открытых учебных документов из MIT~OCW, OpenEdu,
HuggingFace Learn и открыто доступных материалов каталогов курсов.
Его статус --- \texttt{READY\_FOR\_MEASURED\_BENCHMARK}: корпус
готов к запуску измерений, но сам по себе не является результатом
сравнения качества.

Правило этого контроля: широкое утверждение о том, что ИИ-методист существенно
лучше аналогов, заблокировано до измеренной сравнительной проверки
на открытых курсах $n \geq 30$ с линиями B2, B4 и базовой ручной оценкой
по шкале, а также с исходными JSON/CSV-артефактами и отдельным разделом ограничений.
