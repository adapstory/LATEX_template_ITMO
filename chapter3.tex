\chapter{ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ}

% ─────────────────────────────────────────────────────────────────
\section{Архитектура системы}
% ─────────────────────────────────────────────────────────────────

\subsection{Контекстная диаграмма (C4 Level~1)}

ИИ-методист реализован как продуктовый контур платформы Adapstory,
который объединяет сервис генерации курсов, RAG-поиск, граф знаний,
ручную проверку и публикацию результата в LMS. На уровне системного
контекста (C4~Level~1, приложение~Г, раздел~Г.1) участвуют следующие
акторы и системы:

\begin{itemize}
  \item \textbf{Разработчик курса / методист / HR-специалист} ---
    загружает исходные материалы, задаёт аудиторию и методологию,
    просматривает, корректирует и утверждает результат;
  \item \textbf{School UI и BFF~School} --- веб-интерфейс и
    серверный слой для веб-клиента, через которые пользователь запускает
    генерацию, видит статус, происхождение результата, знак качества и результат;
  \item \textbf{AI~Course~Generator} --- FastAPI-микросервис,
    исполняющий LangGraph-конвейер подготовки курса;
  \item \textbf{Dify/RAG-контур} --- индексирует и ищет фрагменты
    исходных документов для генерации с опорой на источник;
  \item \textbf{Граф знаний} --- предоставляет понятия, связи
    компетенций и дополнительные контекстные факты;
  \item \textbf{Ollama / OpenAI-совместимая конечная точка LLM} ---
    обслуживает локальные модели семейства Qwen в тестовом и env-dev контуре;
  \item \textbf{PostgreSQL} --- хранит запуски, результаты,
    контрольные точки LangGraph и данные аудита / происхождения результата;
  \item \textbf{LMS Adapstory} --- принимает утверждённый курс
    после человеческого контроля качества.
\end{itemize}

\subsection{Диаграмма контейнеров (C4 Level~2)}

На уровне контейнеров (приложение~Г, раздел~Г.2) MVP состоит из
следующих процессов:

\begin{itemize}
  \item \textbf{AI~Course~Generator API} --- FastAPI-сервис с REST
    и MCP-инструментами; принимает запросы от BFF, создаёт запуск,
    отдаёт статус, результат, происхождение и команды проверки человеком;
  \item \textbf{Среда исполнения LangGraph} --- исполняет граф генерации,
    поддерживает точки прерывания и сохраняет состояние через
    PostgreSQL-хранилище контрольных точек;
  \item \textbf{Инструмент Dify/RAG} --- выполняет загрузку,
    индексирование и поиск фрагментов источника; при недоступности
    поиска система переходит к ограниченному резервному фрагменту исходного текста
    и явно помечает снижение полноты доказательного контекста;
  \item \textbf{MCP графа знаний} --- возвращает связанные
    компетенции и понятия, усиливая контекст CourseStructureAgent;
  \item \textbf{Шлюз LLM} --- провайдер-агностичный слой вызова
    модели с повторными попытками, телеметрией токенов и стоимости
    и возможностью переключения между локальной и внешней конечной точкой;
  \item \textbf{School BFF / пользовательский интерфейс} --- пользовательский контур
    проверки, публикации и отображения дипломных метрик.
\end{itemize}

\subsection{Доменная модель}

Доменная модель текущего MVP включает следующие ключевые сущности:

\begin{itemize}
  \item \textbf{CourseRun} --- запуск генерации с tenant/user,
    выбранной методологией, статусом, стоимостью и временем выполнения;
  \item \textbf{SourceDocument} и \textbf{EvidenceTable} ---
    нормализованное представление найденных фрагментов источника
    со стабильными \texttt{source\_id} для ссылок;
  \item \textbf{CourseBlueprint} --- методический черновик структуры
    курса: модули, уроки, цели обучения и Bloom-разметка;
  \item \textbf{LessonContent} --- черновой учебный блок со встроенными
    ссылками на источники;
  \item \textbf{VerificationReport} --- отчёт проверки фактов и
    финальной проверки, содержащий \texttt{total\_claims},
    \texttt{cited\_claims} и \texttt{quality\_seal};
  \item \textbf{ProvenanceRecord} --- запись аудита с моделью,
    версией конвейера, набором контрольных правил, числом источников,
    телеметрией токенов и стоимости и временем генерации;
  \item \textbf{ThesisValidationSnapshot} --- машинно читаемый
    срез метрик H1--H3 для дипломной валидации.
\end{itemize}

% ─────────────────────────────────────────────────────────────────
\section{Мультиагентная система (LangGraph)}
% ─────────────────────────────────────────────────────────────────

\subsection{Граф агентов}

Граф агентов реализован через \texttt{StateGraph}
из LangGraph~\cite{langgraph_docs_2025}. В текущей реализации
используется шестиузловой конвейер, вдохновлённый подходом Фейнмана:

\begin{center}
\texttt{source\_analyzer -> course\_structure -> content\_creator -> fact\_check -> review -> verifier}
\end{center}

В граф встроены две точки участия человека:

\begin{itemize}
  \item после \texttt{course\_structure}: методист утверждает или
    возвращает на доработку структуру курса до генерации полного
    контента;
  \item после \texttt{verifier}: методист видит знак качества,
    происхождение результата и блокирующие замечания перед публикацией.
\end{itemize}

Граф агентов представлен на рисунке~\ref{fig:agent-graph}.

\input{figures/fig-agent-graph.tex}

\subsection{Ноды конвейера}

Конвейер включает шесть специализированных нод:

\begin{enumerate}
  \item \textbf{SourceAnalyzer} --- извлекает из Dify/RAG релевантные
    фрагменты, строит \texttt{EvidenceTable} и помечает неполное
    покрытие источников; граф знаний подключается на следующем
    шаге как семантическое усиление \texttt{CourseStructureAgent};
  \item \textbf{CourseStructureAgent} --- формирует
    \texttt{CourseBlueprint} по методологии ADDIE, целевой аудитории
    и таблице свидетельств;
  \item \textbf{ContentCreatorAgent} --- пишет черновые уроки
    со встроенными ссылками на \texttt{source\_id};
  \item \textbf{FactCheckAgent} --- проверяет фактические утверждения
    относительно EvidenceTable и выделяет неподтверждённые утверждения;
  \item \textbf{ReviewAgent} --- оценивает педагогическую связность,
    покрытие Блума и соответствие методологическому контрольному списку;
  \item \textbf{Verifier} --- алгоритмически считает долю
    подтверждённых утверждений, формирует \texttt{PASS},
    \texttt{PASS\_WITH\_NOTES} или \texttt{FAIL}, собирает
    происхождение результата и снимок H1--H3.
\end{enumerate}

\subsection{Управление состоянием и участие человека в контуре}

Состояние графа хранится в \texttt{CourseGenerationState}. Оно
содержит входные параметры, \texttt{source\_documents},
\texttt{evidence\_table}, \texttt{blueprint},
\texttt{lesson\_contents}, \texttt{fact\_check\_reports},
\texttt{review\_report}, \texttt{verification\_report},
\texttt{provenance}, телеметрию токенов и стоимости и обратную связь человека.

В контуре, приближённом к промышленному, граф компилируется с PostgreSQL-хранилищем контрольных точек.
Это даёт три свойства, критичные для B2B-сценария:

\begin{itemize}
  \item запуск можно возобновить после сбоя сервиса без потери
    промежуточных артефактов;
  \item проверка человеком не требует держать процесс в памяти;
  \item статус запуска, знак качества и происхождение результата доступны через API
    как воспроизводимый артефакт, а не только как текст в UI.
\end{itemize}

% ─────────────────────────────────────────────────────────────────
\section{Ключевые конвейеры}
% ─────────────────────────────────────────────────────────────────

\subsection{Обработка источника и поиск}

В текущем MVP контур обработки источника устроен так:
индексация и поиск вынесены в инструмент Dify/RAG,
а семантическое усиление структуры выполняется через граф знаний.
Qdrant остаётся допустимой векторной подсистемой в инфраструктурном
профиле, но не заявляется как обязательный реализованный слой
дипломного MVP.

Текущий поток обработки источника:

\begin{enumerate}
  \item пользователь загружает документ через School UI / BFF;
  \item Dify/RAG-контур создаёт или обновляет индекс источника;
  \item \texttt{source\_analyzer} выполняет поиск фрагментов
    и строит \texttt{EvidenceTable}; если индекс временно недоступен,
    используется ограниченный резервный режим по фрагментам исходного текста;
  \item граф знаний добавляет связанные компетенции и понятия
    на этапе проектирования структуры курса;
  \item узлы LangGraph используют общий доказательный контекст, а не
    независимые несвязанные RAG-вызовы.
\end{enumerate}

Последовательность компонентов показана на рисунке~\ref{fig:ingestion-pipeline}.

\input{figures/fig-ingestion-pipeline.tex}

\subsection{Подготовка черновика курса}

Подготовка курса начинается после появления доказательного контекста.
Сценарий взаимодействия компонентов представлен на
рисунке~\ref{fig:course-sequence}.

\input{figures/fig-course-sequence.tex}

Ключевое отличие от одношаговой базовой конфигурации B2 состоит в том, что
система не передаёт весь документ в один запрос. Вместо этого она:

\begin{itemize}
  \item выделяет и нумерует источники до генерации курса;
  \item сначала проектирует структуру, а затем отдаёт её человеку
    на проверка;
  \item генерирует контент уже после утверждения структуры;
  \item проверяет утверждения и блокирует публикацию при низком качестве
    ссылочной привязки.
\end{itemize}

\subsection[Метка качества, прослеживаемость и валидация ВКР]{Метка качества, прослеживаемость и валидация гипотез диплома}

Финальный узел \texttt{verifier} не вызывает LLM. Он алгоритмически
считает отношение \texttt{cited\_claims / total\_claims} и назначает
знак качества:

\begin{itemize}
  \item \texttt{PASS}, если ссылочная привязка утверждений не ниже 90\,\%;
  \item \texttt{PASS\_WITH\_NOTES}, если она находится в диапазоне
    70--90\,\%;
  \item \texttt{FAIL}, если доказательная база отсутствует или качество ниже
    порога.
\end{itemize}

В результате API отдаёт не только текст курса, но и набор проверочных
артефактов: \texttt{citations}, \texttt{quality\_seal},
\texttt{block\_provenance}, \texttt{cost\_quality} и
\texttt{thesis\_validation}. Эти поля используются фронтовыми
E2E-тестами и дипломным скриптом продуктовой валидации, что делает
проверку H1--H3 воспроизводимой.

\subsection{Компоненты плана развития}

Мультимедиа-генерация (FLUX / Silero) не включается в статус
завершённого MVP. Профили SAM / Backward~Design и контур
автоактуализации присутствуют как инженерные заделы, но количественные
результаты диплома сознательно опираются только на парный B2/B4 по ADDIE,
лабораторную оценку, знак качества и привязку к источнику, чтобы не смешивать
готовые измерения с планом развития.

% ─────────────────────────────────────────────────────────────────
\section{Инфраструктура и развёртывание}
% ─────────────────────────────────────────────────────────────────

\subsection{Стенд для тестирования и промышленной эксплуатации}

Для локальной и env-dev проверки используется docker-compose-контур
\texttt{docker-compose.ai-methodist.yml} и overlay
\texttt{docker-compose.ai-methodist.env-dev.yml}. Он поднимает
AI~Course~Generator, AI~Methodist MCP-сервис, инструмент Dify/RAG,
граф знаний, Neo4j, PostgreSQL и Redis. В base-compose задана
лёгкая локальная модель, а env-dev-профиль использует более сильный
Qwen3:8b через OpenAI-совместимую конечную точку Ollama.

Для инфраструктурного развёртывания платформа сохраняет GitOps-подход:
образ собирается CI/CD, значения deployment описываются в Helm/GitOps,
а ручные изменения в Kubernetes не используются как способ доставки
функциональности.

\subsection{Наблюдаемость и проверяемость}

В сервисе используются OpenTelemetry-трассы вокруг узлов LangGraph и
Prometheus-счётчик распределения знака качества. Отдельные инструкции
фиксируют smoke-проверку env-dev, Playwright-проверку с опорой на граф знаний
и матрицу LLM.
Для ВКР это важно: результат генерации становится не демонстрацией
«на словах», а набором машиночитаемых артефактов:

\begin{itemize}
  \item статус запуска и финальный JSON результата;
  \item знак качества и происхождение результата;
  \item снимок проверки дипломных метрик;
  \item сводка Playwright и результаты контрольных прогонов;
  \item отчёт продуктовой проверки по H1--H3.
\end{itemize}
