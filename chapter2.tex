\chapter{ИССЛЕДОВАНИЕ: ЭКСПЕРИМЕНТЫ И ВЫБОР ТЕХНОЛОГИЙ}

В данной главе описывается экспериментальная постановка задачи,
обосновывается выбор технологического стека и фиксируются
\textit{предварительные} результаты текущего MVP.
Цель главы --- не выдать незавершённый pipeline за финальную
валидацию, а показать воспроизводимую исследовательскую логику:
какой корпус собран, какие baseline уже выполнены, какие метрики
подготовлены и почему итоговая архитектура выбрана именно в таком виде.

% ─────────────────────────────────────────────────────────────────
\section{Дизайн эксперимента}
% ─────────────────────────────────────────────────────────────────

\subsection{Тестовый корпус и оценочные артефакты}

Для экспериментов сформирован \textbf{ядровой корпус из 16 документов},
репрезентирующих три основные категории источников, с которыми должен
работать ИИ-методист:

\begin{itemize}
  \item \textbf{6 продуктовых документов Adapstory:} брендбук,
    гайд по маскоту, гайд по мерчу, редакционная политика,
    стилевая рамка бизнес-сегмента и стили HR-бренда;
  \item \textbf{5 корпоративных регламентов:} политика информационной
    безопасности, политика обработки персональных данных, регламент
    управления инцидентами, положение об адаптации сотрудников,
    антикоррупционная политика;
  \item \textbf{5 учебных программ:} syllabi MIT OpenCourseWare
    по информатике, математике, физике и экономике.
\end{itemize}

Такой состав корпуса позволяет проверить систему на трёх существенно
разных типах входа: брендовые и продуктовые документы, нормативные тексты
и академические программы. Именно это соответствует целевым сценариям
продукта: онбординг, compliance и асинхронный учебный курс.

Для автоматизированной оценки подготовлены также два вспомогательных
evaluation-набора:

\begin{itemize}
  \item \textbf{30 QA-пар} (\texttt{qa\_pairs.json}) для последующей
    оценки RAG-компонента на уровне question answering;
  \item \textbf{100 утверждений} (\texttt{statements.json}), из которых
    50 корректных и 50 намеренно искажённых, для оценки факт-чекинга.
\end{itemize}

Важно отметить, что на текущем этапе работы экспертный ground truth
от двух независимых методистов и коэффициент Коэна~$\kappa$ ещё не
сформированы. Для этого уже подготовлен отдельный инструмент
\texttt{calc\_cohens\_kappa.py}, однако сама экспертная разметка
вынесена в следующую итерацию исследования. Поэтому в данной главе
акцент сделан на воспроизводимых автоматизированных метриках,
а не на имитации уже завершённой экспертной валидации.

\subsection{Baseline-конфигурации и зрелость экспериментов}

Для исследования определены четыре конфигурации, сохраняющие полезность
для последующего полного сравнения:

\begin{itemize}
  \item \textbf{B1} --- ручная разработка структуры курса
    методистом-экспертом; используется как верхняя граница качества,
    но пока не прогнана на всём корпусе;
  \item \textbf{B2} --- one-shot генерация на базовой LLM без агентской
    оркестрации; это \textbf{единственный baseline, уже полностью
    прогнанный} на ядровом корпусе;
  \item \textbf{B3} --- коммерческий инструмент класса AI course creator
    (в работе закладывается Coursebox AI как ближайший рыночный аналог);
  \item \textbf{B4} --- ИИ-методист: мультиагентный pipeline, объединяющий
    ingestion, retrieval, проектирование курса, генерацию, проверку
    фактов и ревью.
\end{itemize}

Текущий статус baseline-логики важен для корректной интерпретации
результатов:

\begin{itemize}
  \item \textbf{B2} уже дал воспроизводимые количественные результаты
    по 16 документам;
  \item \textbf{B4} описан архитектурно и поддержан скриптами запуска,
    но на момент последнего прогона не завершил стабильный benchmark-run;
  \item \textbf{B1} и \textbf{B3} оставлены для следующей итерации
    полного сравнительного исследования.
\end{itemize}

Соответственно, текущая версия главы 2 представляет
\textit{промежуточный экспериментальный слой}, а не окончательное
подтверждение гипотез H1--H3.

\subsection{Метрики оценки}

На текущем этапе используются четыре группы метрик:

\begin{itemize}
  \item \textbf{структурные}: валидность JSON-структуры, число модулей,
    число уроков, наличие обязательных полей;
  \item \textbf{педагогические}: Bloom's coverage --- доля из шести
    уровней таксономии Блума, представленных в учебных целях курса;
  \item \textbf{retrieval / factual}: faithfulness, answer relevancy,
    context precision и метрики факт-чекинга;
  \item \textbf{операционные}: время генерации, citation density,
    unit-cost, пригодность к on-premise развёртыванию.
\end{itemize}

Такой набор метрик позволяет одновременно измерять форму результата,
его педагогическую глубину, связь с источником и эксплуатационную
стоимость решения.

% ─────────────────────────────────────────────────────────────────
\section{Выбор и обоснование технологий}
% ─────────────────────────────────────────────────────────────────

\subsection{Фреймворк мультиагентной оркестрации}

Задача ИИ-методиста относится к классу stateful workflow:
документ проходит этапы ingestion, анализа источника, генерации
структуры, наполнения контентом, проверки фактов, ревью и публикации.
Часть шагов выполняется последовательно, часть --- с условными ветками,
остановками и повторным запуском после human-in-the-loop решения.

В ходе отбора рассматривались LangGraph~1.x, AutoGen~0.4,
CrewAI~0.9, Agno~1.x и OpenAI Agents SDK
~\cite{langgraph_docs_2025, autogen_04_2024, crewai_docs_2025,
agno_docs_2025, openai_agents_sdk_2025}.

\begin{table}[h!]
  \caption{Сравнение фреймворков мультиагентной оркестрации}
  \label{tab:frameworks}
  \centering
  \begin{tabular}{|p{4.2cm}|p{1.8cm}|p{1.8cm}|p{1.8cm}|p{1.7cm}|p{1.8cm}|}
  \hline
  \textbf{Критерий}
    & \textbf{LangGraph 1.x}
    & \textbf{AutoGen 0.4}
    & \textbf{CrewAI 0.9}
    & \textbf{Agno 1.x}
    & \textbf{OAI Agents SDK} \\
  \hline
  Модель исполнения
    & Stateful DAG (StateGraph)
    & Conversation-centric actors
    & Ролевая модель (Crew)
    & Async-first агенты
    & Tool-calling loop \\
  \hline
  Явный граф переходов
    & \textbf{+++}
    & ++
    & +
    & ++
    & + \\
  \hline
  Поддержка локальных LLM
    & \textbf{+}
    & \textbf{+}
    & \textbf{+}
    & \textbf{+}
    & --- \\
  \hline
  Human-in-the-loop
    & \textbf{+++}
    & ++
    & +
    & ++
    & + \\
  \hline
  Наблюдаемость и трассировка
    & \textbf{+++}
    & ++
    & +
    & ++
    & ++ \\
  \hline
  Зрелость экосистемы
    & \textbf{+++}
    & ++
    & ++
    & +
    & ++ \\
  \hline
  \end{tabular}
\end{table}

\textbf{Обоснование выбора LangGraph.}
Из всех кандидатов именно LangGraph лучше всего соответствует природе
задачи: система должна хранить состояние workflow, поддерживать
возвраты к предыдущим шагам, вставки human review и трассировку
каждого узла. Для этого \texttt{StateGraph} подходит лучше, чем
conversation-centric модели. Дополнительным фактором стала интеграция
с LangSmith, которая даёт наблюдаемость на уровне отдельных вызовов
агентов и позволяет дебажить качество генерации.

\textbf{Причины отказа от альтернатив.}
AutoGen силён как среда многоагентного взаимодействия, но для
детерминированного pipeline требует дополнительных абстракций над
conversation-моделью. CrewAI удобно описывает роли и задачи, однако
хуже соответствует продакшн-ориентированному async workflow.
Agno привлекателен своей асинхронностью и RAG-встроенностью, но
уступает в зрелости экосистемы. OpenAI Agents SDK не рассматривается
как базовый вариант из-за облачной привязки и отсутствия on-premise
траектории.

\subsection{Гибридная архитектура: Haystack~2.x + LangGraph}

После выбора оркестратора возникает второй вопрос: чем строить
слой document ingestion и retrieval. В данной работе принят
\textbf{гибридный подход}: Haystack~2.x отвечает за ingestion,
embedding и подготовку документов к retrieval, а LangGraph ---
за stateful агентную оркестрацию поверх уже индексированного корпуса.

Такое разделение ролей соответствует сильным сторонам обоих
фреймворков. Haystack даёт компонентную архитектуру ingestion-pipeline,
где каждый шаг --- конвертация, очистка, чанкинг, эмбеддинг, запись
в векторное хранилище --- оформлен как отдельный модуль с явным
контрактом входа и выхода~\cite{haystack2_docs_2024}.
LangGraph, напротив, сильнее на уровне агентного процесса,
где необходимо хранить состояние курса и результаты промежуточных
агентов между шагами.

Итоговая связка выглядит так:
\texttt{Document source -> Haystack AsyncPipeline -> embeddings ->
Qdrant -> LangGraph workflow -> course artifacts}.
Это снижает связанность системы и упрощает дальнейшую замену
отдельных компонентов без переписывания всей архитектуры.

\subsection{Большая языковая модель}

На этапе экспериментального MVP сравнивались три направления:
облачная модель общего назначения, российский облачный провайдер
и локальная открытая модель.

\begin{table}[h!]
  \caption{Сравнение LLM-вариантов для текущей итерации}
  \label{tab:llms}
  \centering
  \begin{tabular}{|p{4cm}|p{3.2cm}|p{3.2cm}|p{3.2cm}|}
  \hline
  \textbf{Критерий}
    & \textbf{GPT-4o}
    & \textbf{YandexGPT 4}
    & \textbf{Qwen2.5 7B} \\
  \hline
  Способ доступа
    & облачный API
    & облачный API
    & \textbf{локально через Ollama} \\
  \hline
  Суверенитет данных
    & ---
    & $\pm$
    & \textbf{+} \\
  \hline
  Стоимость итераций и отладки
    & высокая
    & средняя
    & \textbf{низкая} \\
  \hline
  Русскоязычный контент
    & высокая
    & высокая
    & достаточная для baseline \\
  \hline
  Инфраструктурная простота
    & высокая
    & высокая
    & \textbf{высокая для локального стенда} \\
  \hline
  \end{tabular}
\end{table}

Для текущих baseline-экспериментов выбрана модель
\texttt{qwen2.5:7b} через Ollama. Причина выбора прагматична:
эта модель доступна локально, даёт воспроизводимые результаты,
не требует внешнего API и позволяет многократно прогонять
эксперименты на ядровом корпусе без роста переменных затрат.

Важно подчеркнуть, что это \textbf{не окончательное ограничение}
архитектуры. ИИ-методист проектируется как provider-agnostic
система: при необходимости локальный baseline может быть заменён
на более крупный open-source вариант семейства Qwen~2.5 или
на облачный провайдер, если сценарий внедрения это допускает.

\subsection{Embedding-модель и векторное хранилище}

Для retrieval-слоя в текущей итерации выбран стек
\texttt{deepvk/USER-bge-m3} + Qdrant.
Выбор объясняется четырьмя причинами:

\begin{itemize}
  \item ориентация модели на русский язык и мультиязычные корпуса;
  \item отсутствие необходимости во внешнем embedding API;
  \item удобная интеграция с Hugging Face / локальным inference-потоком;
  \item наличие Qdrant в инфраструктурном контуре Adapstory
    как production-ready vector DB~\cite{user_bge_m3_2024, qdrant_docs_2024}.
\end{itemize}

Количественный benchmark по MRR@10 и recall@5 подготовлен как
следующий шаг исследования. В текущем черновике выбор фиксируется
как наиболее совместимый с русскоязычным B2B-корпусом и on-premise
требованиями.

\subsection{Стратегии чанкинга}

Для document ingestion были рассмотрены две стратегии:
рекурсивный fixed-size чанкинг и семантический чанкинг.
Хотя полноценный количественный тест по context precision ещё не
завершён, архитектурно более перспективным выбран
\texttt{SemanticDoubleMergingSplitter} из Haystack.

Причина выбора заключается в том, что учебные и нормативные документы
содержат смысловые блоки разной длины: определения, процедуры,
ограничения, списки требований. Жёсткий fixed-size split разрушает
эти границы и создаёт более шумный retrieval-контекст. Семантический
чанкинг лучше сохраняет логические единицы, что особенно важно
для compliance-документов и брендовых руководств.

\subsection{Модели генерации изображений}

Для мультимедийного слоя сравнивались DALL-E~3 и FLUX.1-dev
~\cite{dalle3_2023, flux_2024}. DALL-E~3 удобен как внешний сервис
с высоким качеством и быстрым стартом, но вносит в систему
API-зависимость и переменные затраты. FLUX.1-dev допускает локальное
развёртывание и потому лучше соответствует логике on-premise продукта.

В результате для текущего MVP выбрана следующая стратегия:

\begin{itemize}
  \item \textbf{по умолчанию} --- локальный FLUX.1-dev для черновых
    и полуфинальных образовательных иллюстраций;
  \item \textbf{опционально} --- внешний провайдер класса DALL-E~3
    для premium-сценариев, где требуется более «маркетинговое»
    качество изображения.
\end{itemize}

Такое решение будущесовместимо: оно не закрывает дорогу к внешнему
качественному генератору, но сохраняет базовый on-premise маршрут
для большинства B2B-кейсов.

\subsection{TTS-движки для генерации подкастов}

Для генерации русскоязычных подкастов сравнивались Silero~TTS~v4
и ElevenLabs Multilingual~v2~\cite{silero_tts_2024, elevenlabs_2024}.
На текущем этапе различие между ними определяется прежде всего
не субъективным MOS, а эксплуатационной логикой.

\begin{itemize}
  \item \textbf{Silero~TTS} подходит для on-premise развёртывания,
    не требует внешнего API и по заложенной в benchmark-модель
    оценке даёт стоимость порядка 4~руб.\ на курс за счёт только
    GPU-времени.
  \item \textbf{ElevenLabs} обеспечивает более «человеческую»
    интонационную подачу, но при оценке 40{,}000 символов на курс
    стоимость составляет порядка 1{,}080~руб.\ на курс, что делает
    его скорее premium-опцией, чем базовым TTS-слоем для MVP.
\end{itemize}

Панельная MOS-оценка пока не завершена. Для этого уже подготовлен
отдельный benchmark-скрипт, однако в текущей итерации для выбора
технологии решающими оказались суверенитет данных и предсказуемость
себестоимости. Поэтому базовым TTS-движком выбран Silero.

% ─────────────────────────────────────────────────────────────────
\section{Промежуточные результаты экспериментов}
\label{sec:exp-results}
% ─────────────────────────────────────────────────────────────────

\subsection{Предварительные результаты baseline B2}

Первым полностью воспроизводимым экспериментом стал прогон
one-shot baseline B2 на ядровом корпусе из 16 документов.
В этом baseline использовалась локальная модель \texttt{qwen2.5:7b},
которая по одному промпту генерировала структуру курса в JSON-формате
без retrieval, без факт-чекинга и без отдельных специализированных
агентов.

\begin{table}[h!]
  \caption{Предварительные результаты baseline B2 на ядровом корпусе}
  \label{tab:b2prelim}
  \centering
  \begin{tabular}{|p{4.0cm}|p{1.5cm}|p{2.2cm}|p{2.2cm}|p{2.5cm}|p{2.0cm}|}
  \hline
  \textbf{Срез корпуса}
    & \textbf{$n$}
    & \textbf{Среднее время, c}
    & \textbf{Среднее Bloom, \%}
    & \textbf{Доля курсов $\geq 70\%$}
    & \textbf{Valid structure} \\
  \hline
  Все документы
    & 16
    & 4.31
    & 61.5
    & 18.8\%
    & 100\% \\
  \hline
  Продуктовые документы
    & 6
    & 5.28
    & 75.0
    & 33.3\%
    & 100\% \\
  \hline
  Регламенты
    & 5
    & 3.76
    & 40.0
    & 0\%
    & 100\% \\
  \hline
  Учебные программы
    & 5
    & 3.70
    & 66.7
    & 20.0\%
    & 100\% \\
  \hline
  \end{tabular}
\end{table}

Полученные данные показывают, что one-shot baseline обладает двумя
сильными сторонами: он очень быстро генерирует структурно валидный
ответ и не требует сложной инфраструктуры. Однако эти преимущества
не решают ключевую задачу исследования.

Во-первых, среднее покрытие Bloom's taxonomy составляет лишь 61.5\,\%,
а порог 70\,\% достигается только в 18.8\,\% случаев. Во-вторых,
baseline значительно лучше справляется с продуктово-описательными
документами, чем с нормативными: для регламентов покрытие падает
до 40\,\%, что означает смещение к нижним когнитивным уровням
remember/understand. В-третьих, citation density равна нулю:
модель не даёт никакой явной привязки результата к источнику,
поэтому baseline не может считаться достаточным для compliance-
или knowledge-critical сценариев.

Именно этот набор ограничений и мотивирует переход от B2 к B4:
необходима архитектура, которая сохраняет скорость, но усиливает
retrieval, факт-чекинг и глубину методической структуры.

\subsection{Подготовленные оценочные наборы для RAG и факт-чекинга}

Несмотря на то что полный B4-benchmark ещё не завершён, исследование уже
подготовило воспроизводимую основу для дальнейшей оценки RAG-компонента
и fact-check слоя.

\begin{table}[h!]
  \caption{Подготовленные оценочные наборы текущего MVP}
  \label{tab:evalassets}
  \centering
  \begin{tabular}{|p{5.5cm}|p{2.5cm}|p{6.0cm}|}
  \hline
  \textbf{Артефакт}
    & \textbf{Объём}
    & \textbf{Назначение} \\
  \hline
  Ядровой корпус документов
    & 16
    & baseline- и pipeline-генерация курсов \\
  \hline
  QA-пары (\texttt{qa\_pairs.json})
    & 30
    & последующая оценка faithfulness и answer relevancy \\
  \hline
  Фактуальные утверждения (\texttt{statements.json})
    & 100
    & оценка fact-check precision / recall / F1 \\
  \hline
  \end{tabular}
\end{table}

Эти артефакты важны тем, что превращают работу из описательной
архитектурной записки в экспериментально воспроизводимую постановку:
следующая итерация не начинается «с нуля», а опирается на уже
подготовленный корпус и формализованные benchmark-наборы.

\subsection{Ограничения текущего этапа и план ablation-исследования}

Наиболее существенное ограничение текущего этапа состоит в том, что
полный прогон B4 ещё не стабилизирован как исследовательский benchmark.
Для pipeline уже подготовлены скрипты baseline comparison,
Bloom evaluation, RAG evaluation, fact-check evaluation и ablation study,
однако на момент последнего прогона мультиагентный endpoint не завершил
полный сравнительный цикл.

Это ограничение не обесценивает проделанную работу, но накладывает
важную границу на интерпретацию результатов:

\begin{itemize}
  \item текущие цифры chapter 2 следует трактовать как
    \textbf{baseline-слой и технологическую разведку};
  \item полная проверка гипотез H1--H3 требует завершения B4-progons,
    экспертного ground truth и последующего B1/B3/B4 сравнения;
  \item ablation study остаётся ключевым следующим экспериментом,
    так как именно он покажет вклад retrieval-, fact-check- и
    review-агентов в итоговое качество.
\end{itemize}

Таким образом, chapter 2 фиксирует не только успехи, но и текущую
границу зрелости системы --- это делает дальнейшее развитие текста
и экспериментов прозрачным и аккуратным.

\subsection{Финальная конфигурация следующей итерации}

На основании проведённого отбора и предварительных результатов
следующая итерация ИИ-методиста должна строиться на следующей
конфигурации:

\begin{itemize}
  \item \textbf{LLM baseline / local core}: Qwen2.5 через Ollama;
  \item \textbf{Оркестратор}: LangGraph~1.x (\texttt{StateGraph});
  \item \textbf{Document Ingestion}: Haystack~2.x AsyncPipeline;
  \item \textbf{Embedding}: \texttt{deepvk/USER-bge-m3};
  \item \textbf{Vector Store}: Qdrant;
  \item \textbf{Чанкинг}: семантический;
  \item \textbf{Image generation}: FLUX.1-dev как базовый локальный путь;
  \item \textbf{TTS}: Silero~TTS как базовый on-premise движок.
\end{itemize}

Именно эта конфигурация наиболее последовательно соответствует целям
работы: on-premise развёртывание, воспроизводимые эксперименты,
приемлемая себестоимость, возможность усиления retrieval и
педагогической структуры по сравнению с one-shot baseline.
